{
  "name": "gridline",
  "version": "0.1.0",
  "description": "Gridline — ranked iRacing with its own independent ranking system",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}